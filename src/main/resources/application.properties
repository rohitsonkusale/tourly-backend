# ===============================
# SERVER CONFIG
# ===============================
server.port=8080

# ===============================
# DATABASE CONFIG
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/tourly_db
spring.datasource.username=tourly_user
spring.datasource.password=Tourly@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE CONFIG
# ===============================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# ===============================
# FLYWAY CONFIG
# ===============================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=5

# ===============================
# JWT CONFIG
# ===============================
jwt.secret=TourlySuperSecureJWTSecretKeyForProduction12345
jwt.expiration=3600000

# ===============================
# RAZORPAY CONFIG
# (Used by RazorpayConfig bean + Webhook verification)
# ===============================
razorpay.key=rzp_test_xxxxxxxxx
razorpay.secret=xxxxxxxxxxxx
razorpay.webhook.secret=your_webhook_secret

# ===============================
# FILE UPLOAD CONFIG
# ===============================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# Cloudinary config (frontend uploads directly — backend stores URLs only)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:roamaya}
cloudinary.upload-preset=${CLOUDINARY_UPLOAD_PRESET:roamaya_trips}

# Increased limits for rich trip creation JSON payload
server.tomcat.max-http-form-post-size=50MB
server.tomcat.max-swallow-size=50MB
server.tomcat.connection-timeout=120000
file.upload-dir=uploads/

# ===============================
# JACKSON CONFIG
# ===============================
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Asia/Kolkata

# ===============================
# LOGGING CONFIG
# ===============================
logging.level.root=INFO
logging.level.com.tourly=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===============================
# SWAGGER / SPRINGDOC CONFIG
# ===============================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha

# ===============================
# OPTIONAL: ACTUATOR (enable later if added dependency)
# ===============================
# management.endpoints.web.exposure.include=health,info
# management.endpoint.health.show-details=always

# ===============================
# CLOUDINARY CONFIG
# ===============================
cloudinary.cloud-name=YOUR_CLOUD_NAME
cloudinary.upload-preset=roamaya_trips
google.client-id=1055994397285-7qpej7luor7q98mhv0tkenmh8eiqopst.apps.googleusercontent.com
google.client-secret=YOUR_GOOGLE_CLIENT_SECRET
google.redirect-uri=http://localhost:8080/api/auth/google/callback